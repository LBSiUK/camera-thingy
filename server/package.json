{
  "name": "remote-camera-server",
  "version": "1.0.0",
  "description": "Remote camera trigger and photo storage server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.1.1",
    "ws": "^8.16.0"
  }
}
